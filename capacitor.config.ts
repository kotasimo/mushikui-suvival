import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kotas.mushikui.simple',
  appName: 'mushikuiSimple',
  webDir: 'dist'
};

export default config;
